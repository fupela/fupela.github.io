# 2026-04-14

- Mission Control already has Gumroad revenue plumbing in `mission-control/src/app/api/revenue/route.ts`; blocker is Google Sheets auth not fully configured (`GOOGLE_SERVICE_ACCOUNT_EMAIL` and `GOOGLE_PRIVATE_KEY` still needed in `mission-control/.env.local`).
- Batch 3 progress: Muse completed `content/stack-guide-launch-social.md` with X, LinkedIn, and Reddit-style launch drafts focused on the OpenClaw + Cowork two-agent stack.
- Batch 3 progress: Sentinel completed `reputation-monitor/scan-001.md`. Key findings: Jedaiflow has near-zero web footprint, Gumroad product has no visible external pickup yet, OpenClaw/Cowork ecosystem sentiment looks healthy, and the Claude cutoff narrative is still a relevant marketing hook.
- Website deploy root cause identified: live `jedaiflow.com` appears to be served from `fupela/fupela.github.io` behind Cloudflare, not from `fupela/jedaiflow-website`. The blog article 404 is likely because it was prepared in the wrong repo.
- John updated the outreach approval rule: Pela can send outreach emails without prior review unless something glaring suggests John should review first, like awkward wording or risky phrasing.
# 2026-04-14

- Mission Control already has Gumroad revenue plumbing in `mission-control/src/app/api/revenue/route.ts`; blocker is Google Sheets auth not fully configured (`GOOGLE_SERVICE_ACCOUNT_EMAIL` and `GOOGLE_PRIVATE_KEY` still needed in `mission-control/.env.local`).
- Batch 3 progress: Muse completed `content/stack-guide-launch-social.md` with X, LinkedIn, and Reddit-style launch drafts focused on the OpenClaw + Cowork two-agent stack.
- Batch 3 progress: Sentinel completed `reputation-monitor/scan-001.md`. Key findings: Jedaiflow has near-zero web footprint, Gumroad product has no visible external pickup yet, OpenClaw/Cowork ecosystem sentiment looks healthy, and the Claude cutoff narrative is still a relevant marketing hook.
- Website deploy root cause identified: live `jedaiflow.com` appears to be served from `fupela/fupela.github.io` behind Cloudflare, not from `fupela/jedaiflow-website`. The blog article 404 is likely because it was prepared in the wrong repo.
- John updated the outreach approval rule: Pela can send outreach emails without prior review unless something glaring suggests John should review first, like awkward wording or risky phrasing.
- OpenClaw config cleanup: removed `ollama/glm-5.1:cloud` from `/Users/fupie/.openclaw/openclaw.json` defaults and Ollama model list, with default primary set to `openai-codex/gpt-5.4`.
- Hermes setup issue is separate from OpenClaw gateway config: Hermes was still trying to call `glm-5.1:cloud` directly via provider `custom` at `http://127.0.0.1:11434`, producing HTTP 404. Likely a model/provider mismatch in Hermes, not an API key problem.
- Guidance for Hermes: if John wants to use GLM 5.1 there, prefer an explicit Ollama provider/backend if Hermes supports it rather than generic `custom`; otherwise swap Hermes to a different known-good model.
- Recommended Hermes terminal backend on macOS: choose the plain native/system PTY option first, avoid custom/experimental backends unless needed.
- Jolie (GLM-5.1 Hermes agent) bridge cron check: no messages addressed to jolie. Bridge has 17 unread messages (all fupie↔claude/john traffic). Batch 3 at 6/8 — remaining items are SEND (blocked on Gmail/Make.com) and Pela social polish. HOLD_SENDS flag absent. Jolie posted bridge check report marking intro as read.
